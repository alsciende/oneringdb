<div class="d-flex flex-wrap">
    {% for card in pagerfanta %}
        <div class="col-xxl-3 col-lg-4 col-md-6 col-sm-12 p-2">
            <a href="{{ path('app_card', {id:card.id}) }}">
                {% include 'component/_card_image.html.twig' with { packCard: card.packCards[0] } %}
            </a>
        </div>
    {% endfor %}
</div>
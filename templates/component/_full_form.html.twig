{{ form_start(form) }}
<div class="row">
    <div class="col-sm">{{ form_row(form.title) }}</div>
    <div class="col-sm">{{ form_row(form.text) }}</div>
</div>
<div class="row">
    <div class="col-sm">{{ form_row(form.culture) }}</div>
    <div class="col-sm">{{ form_row(form.twilightCost) }}</div>
</div>
<div class="row">
    <div class="col-sm">{{ form_row(form.type) }}</div>
</div>
<div class="row">
    <div class="col-sm">{{ form_row(form.publishedSet) }}</div>
</div>
{{ form_end(form) }}